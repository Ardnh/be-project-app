name: Deploy to VPS (Simple)

on:
    push:
        branches:
            - staging

jobs:
    build-and-deploy:
        runs-on: ubuntu-latest

        steps:
            # 1. Checkout source code
            - name: Checkout code
              run: echo "Checkout code"

            # 2. Login ke Docker Hub
            - name: Login to Docker Hub
              run: echo "Login to Docker Hub"

            # 3. Build Docker image
            - name: Build Docker image
              run: echo "Building Docker image..."

            # 4. Push image ke Docker Hub
            - name: Push Docker image
              run: echo "Pushing Docker image..."

            # 5. Trigger webhook ke VPS
            - name: Trigger VPS Webhook
              run: echo "Triggering VPS webhook..."
